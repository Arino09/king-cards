# 进度文档

## 2026-04-18 · 前端单机卡牌对弈（单 Agent 汇总）

- 初始化 Vite + React + TypeScript + Vitest，集成 Zustand 与路径别名 `@/`。
- 实现基础牌池七种牌的比较链、游戏状态机、`resolvePlay` 结算与终局判定；`src/game/special.ts` 为功能牌占位。
- 实现机器人着法：序贯极小极大向前看若干步（`src/ai/minimax.ts`）+ 局面启发式（`src/ai/evaluate.ts`）。
- 完成组牌界面（十张、须含国王与平民）、对弈界面、弃牌堆与可见/不可见牌组查看、回合结果与终局后直接开始新游戏。
- 单元测试覆盖 `compare` 与 `resolvePlay` 基础行为。

## 2026-04-18 · 根目录 README

- 新建 [`README.md`](../README.md)：说明环境要求、安装、`dev` / `build` / `preview`、测试命令及脚本一览，便于成员本地运行与静态部署。

## 2026-04-18 · 牌面显示等级

- 在 [`compare.ts`](../src/game/compare.ts) 增加 `formatRankValue`、`formatCardWithRank`；组牌页、手牌按钮、各列表弹层与回合文案均展示「等级」数值（与 `RANK` 一致）。

## 2026-04-18 · Gitee Pages 部署说明（已由 GitHub Pages 替代）

- 在 [`package.json`](../package.json) 增加 `build:gitee`、`preview:gitee`（`base=/king-cards/`）。
- 在 [`README.md`](../README.md) 增补「部署到 Gitee Pages」：构建、开启 Pages、`dist` 被忽略时需 `git add -f dist/`、仓库改名时调整 `base`。

## 2026-04-18 · GitHub 远程与 GitHub Pages

- Gitee Pages 已下线相关能力，脚本更名为 `build:pages`、`preview:pages`（`base=/king-cards/`）。
- 新增 [`.github/workflows/deploy-pages.yml`](../.github/workflows/deploy-pages.yml)：推送 `main` 时用 Actions 构建并部署 Pages。
- [`README.md`](../README.md) 重写为：添加 `github` 远程、GitHub Pages（Actions / `gh-pages` 手动）、仓库改名时修改 `base`。

## 2026-04-18 · 修复 GitHub Pages Actions 失败

- 工作流升级为 **`actions/deploy-pages@v5`**，并增加 **`actions/configure-pages@v5`**（与官方 Pages 模板一致）；监听 **`main` / `master`**，支持 **`workflow_dispatch`** 手动运行。
- [`README.md`](../README.md) 增补 Actions 失败排查表（`npm ci`、Pages 源、分支、Environment 审批）。
